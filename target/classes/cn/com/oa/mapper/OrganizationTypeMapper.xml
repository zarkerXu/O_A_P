<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.com.oa.mapper.OrganizationTypeMapper" >
<resultMap id="OrganizationType" type="cn.com.oa.model.OrganizationType">
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
      This element was generated on Fri Nov 20 17:32:57 CST 2015.
    -->
    <id column="id" jdbcType="VARCHAR" property="id" />
    <result column="name" jdbcType="VARCHAR" property="name" />
    <result column="remark" jdbcType="VARCHAR" property="remark" />
    <result column="isuse" jdbcType="INTEGER" property="isuse" />
     <result column="sort" jdbcType="INTEGER" property="sort" />
    <result column="cuid" jdbcType="VARCHAR" property="cuid" />
    <result column="createTime" jdbcType="TIMESTAMP" property="createTime" />
    <result column="uuid" jdbcType="VARCHAR" property="uuid" />
    <result column="updataTime" jdbcType="TIMESTAMP" property="updataTime" />
    
    
  </resultMap>
<!--  删除记录  -->  
    <delete id="delete" parameterType="String">
       delete from sys_organization_type where id = #{id}
    </delete>  
     <select id="select" parameterType="OrganizationType" resultType="OrganizationType">
    select * from sys_organization_type where isuse=0 order by sort
    </select>
    <select id="selectone" parameterType="String" resultType="OrganizationType">
    select * from sys_organization_type where isuse=0 and id=#{id} order by sort
    </select>
    <update id="update" parameterType="OrganizationType">
    update sys_organization_type
    <set> 
    <if test="id!=null and id!=''">
     id=#{id},
    </if>
    <if test="name !=null and name !=''">
     name=#{name},
    </if>
    <if test="remark !=null and remark !=''">
     remark=#{remark},
    </if>
    <if test="isuse !=null and isuse !=''">
     isuse=#{isuse},
    </if>
    <if test="cuid !=null and cuid !=''">
     cuid=#{cuid},
    </if>
    <if test="createTime !=null and createTime !=''">
     createTime=#{createTime},
    </if>
    <if test="uuid !=null and uuid !=''">
     uuid=#{uuid},
    </if>
    <if test="updataTime !=null and updataTime !=''">
     updataTime=#{updataTime},
    </if>
    <if test="sort !=null">
     sort=#{sort}
    </if>
    </set>
    where id=#{id}
    </update>
    
    <update id="updatedel" parameterType="OrganizationType">
    update sys_organization_type
    <set> 
     isuse=1
    </set>
    where id=#{id}
    </update>
    
    <insert id="insert" parameterType="OrganizationType">
      insert into sys_organization_type(id,name,remark,isuse,cuid,createTime,uuid,updataTime,sort) values(#{id},#{name,jdbcType=VARCHAR},#{remark,jdbcType=VARCHAR},0,#{cuid},#{createTime},#{uuid},#{updataTime},#{sort})
    </insert>
</mapper>