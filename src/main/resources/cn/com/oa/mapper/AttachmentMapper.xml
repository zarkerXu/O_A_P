<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.com.oa.mapper.AttachmentMapper" >
<resultMap id="Attachment" type="cn.com.oa.model.Attachment">
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
      This element was generated on Fri Nov 20 17:32:57 CST 2015.
    -->
    <id column="id" jdbcType="VARCHAR" property="id" />
    <result column="pid" jdbcType="VARCHAR" property="pid" />
    <result column="type" jdbcType="INTEGER" property="type" />
    <result column="fileName" jdbcType="VARCHAR" property="fileName" />
    <result column="fileSize" jdbcType="VARCHAR" property="fileSize" />
    <result column="suffix" jdbcType="VARCHAR" property="suffix" />
    <result column="url" jdbcType="VARCHAR" property="url" />
    <result column="remark" jdbcType="VARCHAR" property="remark" />
    <result column="isuse" jdbcType="INTEGER" property="isuse" />
    <result column="remark" jdbcType="VARCHAR" property="remark" />
    <result column="cuid" jdbcType="VARCHAR" property="cuid" />
    <result column="createTime" jdbcType="TIMESTAMP" property="createTime" />
    
    
  </resultMap>
<!--  删除记录  -->  
    <delete id="delete" parameterType="String">
       delete from attachment where id = #{id}
    </delete>  
     <select id="select" parameterType="String" resultType="Attachment">
    select * from attachment where id=#{id}
    </select>
    <select id="findByParameter" parameterType="Attachment" resultType="Attachment">
    select * from attachment
    <where>
    <if test="id!=null and id!=''">
    and id=#{id}
    </if>
    <if test="pid !=null and pid !=''">
    and pid=#{pid}
    </if>
    <if test="type !=null">
    and type=#{type}
    </if>
    <if test="fileName !=null and fileName !=''">
    and fileName=#{fileName}
    </if>
    <if test="fileSize !=null and fileSize !=''">
    and fileSize=#{fileSize}
    </if>
    <if test="suffix !=null and suffix !=''">
    and suffix=#{suffix}
    </if>
    <if test="url !=null and url !=''">
    and url=#{url}
    </if>
    <if test="isuse !=null">
    and isuse=#{isuse}
    </if>
    <if test="remark !=null and remark !=''">
    and remark=#{remark}
    </if>
    <if test="cuid !=null and cuid !=''">
    and cuid=#{cuid}
    </if>
    <if test="createTime !=null and createTime !=''">
    and createTime=#{createTime}
    </if>
    </where>
    <if test="orderBy !=null and orderBy !=''">
    order by ${orderBy}
    </if>
    </select>
    
    <insert id="insert" parameterType="Doc">
      insert into attachment(id,pid,type,fileName,fileSize,suffix,url,isuse,remark,cuid,createTime) values(#{id},#{pid,jdbcType=VARCHAR},#{type},#{fileName,jdbcType=VARCHAR},#{fileSize,jdbcType=VARCHAR},#{suffix,jdbcType=VARCHAR},#{url,jdbcType=VARCHAR},0,#{remark,jdbcType=VARCHAR},#{cuid},#{createTime})
    </insert>
</mapper>